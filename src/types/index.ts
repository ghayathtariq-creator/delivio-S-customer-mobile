// TypeScript type definitions for the delivio-S-customer-mobile app

type User = {
    id: string;
    name: string;
    email: string;
};

type Product = {
    id: string;
    name: string;
    price: number;
};

export type { User, Product };